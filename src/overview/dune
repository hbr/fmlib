(library
    (libraries unix fmlib_std fmlib_pretty fmlib_parse fmlib_js)
    (inline_tests)
    (preprocess (pps ppx_inline_test))
    (public_name fmlib)
    (name fmlib))

(documentation
    (mld_files index)
    (package fmlib)
)
